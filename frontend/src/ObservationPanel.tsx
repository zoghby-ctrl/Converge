import { reviewerLabel } from './display'
import React, {useEffect, useState} from 'react'

export type State = 'present' | 'absent' | 'uncertain' | 'not_mentioned'
export type Temporal = 'current' | 'resolved' | 'historical' | 'uncertain'
export type Condition = 'standing_water' | 'road_damage' | 'passage_obstruction' | 'recurrence'
export type Field = Condition | 'reported_duration' | 'reported_explanation' | 'temporal_status'
export type Extraction = Record<Condition, State> & {language: string; temporal_status: Temporal; reported_duration: {minutes: number | null; phrase: string} | null; reported_explanation: string | null; evidence_spans: {field: Field; quote: string; temporal_status: Temporal}[]; uncertainties: {field: Field; reason: string; affects_admission: boolean}[]}
export type ImageState = 'present' | 'absent' | 'uncertain' | 'not_assessable'
export type ImageField = 'standing_water' | 'visible_surface_damage' | 'passage_obstruction'
export type ImageExtraction = {standing_water: ImageState; visible_surface_damage: ImageState; passage_obstruction: ImageState; image_quality: 'usable' | 'limited' | 'unusable'; visible_description: string; uncertainties: {field: ImageField; reason: string}[]; support_descriptions: {field: ImageField; description: string}[]}
export type Revision = {extraction: Extraction; source: string; model: string | null; model_identifier: string | null; fallback_used: boolean; fallback_reason: string | null; review_state: string; processed_at: string; revision: number}
export type ImageRevision = Omit<Revision, 'extraction'> & {extraction: ImageExtraction; provider?: string | null; cache_status?: string | null}
export type Job = {signal_id: string; kind?: 'text' | 'image'; original: {text?: string; operator: string; content_origin: string; placement_origin?: string; time_origin?: string; image_url?: string | null; raw_hash?: string | null; normalization?: Record<string, unknown> | null; capture_group_id?: string}; status: string; error_code: string | null; revision: number; latest: Revision | ImageRevision | null; revisions: (Revision | ImageRevision)[]; disposition: string; excluded: {rule_ids: string[]}[]; paired_text_signal_id?: string | null; linked_text_signal_id?: string | null; paired_text_job?: Job | null}
export const conditions: Condition[] = ['standing_water', 'road_damage', 'passage_obstruction', 'recurrence']
export const imageFields: ImageField[] = ['standing_water', 'visible_surface_damage', 'passage_obstruction']
export const human = (s: string) => s.replaceAll('_', ' ')
export const imageLabel = (s: string) => s === 'visible_surface_damage' ? 'Road surface damage' : human(s)
export const emptyExtraction: Extraction = {language: 'unknown', standing_water: 'not_mentioned', road_damage: 'not_mentioned', passage_obstruction: 'not_mentioned', recurrence: 'not_mentioned', reported_duration: null, reported_explanation: null, temporal_status: 'uncertain', evidence_spans: [], uncertainties: []}
export const errorText: Record<string, string> = {invalid_api_key: 'OpenAI rejected the API key. Check the server .env; no automatic retry was made.', api_key_missing: 'Add the API key to the server .env and restart the backend.', api_unavailable: 'OpenAI is unreachable. Saved results and local replay remain available.', api_rate_or_credit_limit: 'OpenAI returned a rate or credit limit. Review the account before retrying.', model_unavailable: 'The configured model is unavailable to this API account.', api_configuration_rejected: 'OpenAI rejected the server request configuration.', fallback_disabled: 'Extraction requires review; Terra fallback is disabled.', development_usage_limit: 'The prototype analysis limit was reached. Saved results and manual review remain available.', schema_invalid: 'Model output failed validation. No new evidence was admitted.', interrupted_restart: 'Processing was interrupted by a restart. Retry explicitly or review manually.', image_too_large: 'Choose a JPEG or PNG no larger than 5 MB.', unsupported_image_format: 'Only JPEG and PNG images are accepted.', image_pixel_limit: 'This image is larger than the 12 megapixel limit.', invalid_image: 'The image could not be decoded safely.'}
export function isImageJob(job: Job): boolean {return job.kind === 'image' || Boolean(job.original.image_url || job.original.raw_hash)}
export function imageUrl(job: Job): string | null {return job.original.image_url ?? null}
export async function api<T>(path: string, body?: unknown): Promise<T> {const r = await fetch('/api/v1/' + path, body ? {method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(body)} : undefined); if (!r.ok) throw new Error(r.status === 409 ? 'The observation changed or is processing. Reload it before editing.' : `Request rejected (${r.status}). Check the fields and source quotes.`); return r.json()}
export async function uploadImage<T>(file: File, metadata: Record<string, unknown>): Promise<T> {const form = new FormData(); form.append('image', file); form.append('metadata', JSON.stringify(metadata)); const r = await fetch('/api/v1/images', {method: 'POST', body: form}); if (!r.ok) {let detail = ''; try {detail = (await r.json()).detail ?? ''} catch {/* non-JSON error */} throw new Error(errorText[detail] ?? (detail || `Image upload rejected (${r.status}).`))} return r.json()}

export function ReviewForm({job, saved}: {job: Job; saved: (job: Job) => void}) {
  const [value, setValue] = useState<Extraction>(structuredClone((job.latest as Revision | null)?.extraction ?? emptyExtraction)); const [reviewer, setReviewer] = useState(reviewerLabel(job.original.operator)); const [reason, setReason] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false)
  function change(field: Condition, state: State) {const next = structuredClone(value); next[field] = state; next.uncertainties = next.uncertainties.filter(u => u.field !== field); if (state === 'not_mentioned') next.evidence_spans = next.evidence_spans.filter(s => s.field !== field); else if (!next.evidence_spans.some(s => s.field === field)) next.evidence_spans.push({field, quote: '', temporal_status: 'current'}); if (state === 'uncertain') next.uncertainties.push({field, reason: 'ambiguous_wording', affects_admission: true}); setValue(next)}
  function extra(field: 'reported_duration' | 'reported_explanation', content: string) {const next = structuredClone(value); if (field === 'reported_duration') next.reported_duration = content ? {phrase: content, minutes: next.reported_duration?.minutes ?? null} : null; else next.reported_explanation = content || null; if (!content) {next.evidence_spans = next.evidence_spans.filter(s => s.field !== field); next.uncertainties = next.uncertainties.filter(u => u.field !== field)} else if (!next.evidence_spans.some(s => s.field === field)) next.evidence_spans.push({field, quote: field === 'reported_duration' ? content : '', temporal_status: next.temporal_status}); if (field === 'reported_duration') next.evidence_spans = next.evidence_spans.map(s => s.field === field ? {...s, quote: content} : s); setValue(next)}
  async function submit(e: React.FormEvent) {e.preventDefault(); setBusy(true); setError(''); try {saved(await api<Job>(`signals/${job.signal_id}/review`, {extraction: value, reviewer, reason, expected_revision: job.revision}))} catch (e) {setError(String(e))} finally {setBusy(false)}}
  return <form className="review-form" onSubmit={submit}><h3>Human correction · new revision</h3><p>Quote the original report. Review confirms what the report says; it does not verify the physical condition.</p><div className="observation-grid">{conditions.map(field => <label key={field}>{human(field)}<select aria-label={`Correct ${human(field)}`} value={value[field]} onChange={e => change(field, e.target.value as State)}>{['present', 'absent', 'uncertain', 'not_mentioned'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label>)}<label>Language<select value={value.language} onChange={e => setValue({...value, language: e.target.value})}>{['egyptian_arabic', 'standard_arabic', 'english', 'mixed', 'unknown'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label><label>Episode time<select value={value.temporal_status} onChange={e => setValue({...value, temporal_status: e.target.value as Temporal})}>{['current', 'resolved', 'historical', 'uncertain'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label></div>{value.evidence_spans.map((span, i) => <div className="observation-grid" key={i}><label>{human(span.field)} source quote<input required maxLength={180} value={span.quote} dir="auto" onChange={e => {const next = structuredClone(value); next.evidence_spans[i].quote = e.target.value; setValue(next)}}/></label><label>Claim time<select value={span.temporal_status} onChange={e => {const next = structuredClone(value); next.evidence_spans[i].temporal_status = e.target.value as Temporal; setValue(next)}}>{['current', 'resolved', 'historical', 'uncertain'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label></div>)}<details><summary>Duration, explanation and uncertainty</summary><p>These annotations do not establish persistence or verified cause.</p><label>Duration phrase from report<input maxLength={120} value={value.reported_duration?.phrase ?? ''} onChange={e => extra('reported_duration', e.target.value)}/></label>{value.reported_duration && <label>Duration in minutes (leave empty if unquantified)<input type="number" min="0" max="5256000" value={value.reported_duration.minutes ?? ''} onChange={e => setValue({...value, reported_duration: {...value.reported_duration!, minutes: e.target.value === '' ? null : Number(e.target.value)}})}/></label>}<label>Reported explanation<input maxLength={240} value={value.reported_explanation ?? ''} onChange={e => extra('reported_explanation', e.target.value)}/></label>{value.uncertainties.map((u,i) => <p key={i}>{human(u.field)} · {human(u.reason)} <button type="button" onClick={() => setValue({...value, uncertainties: value.uncertainties.filter((_,n) => n !== i)})}>Remove resolved uncertainty</button></p>)}</details><label>Reviewer<input required value={reviewer} maxLength={80} onChange={e => setReviewer(e.target.value)}/></label><label>Correction reason<input required value={reason} maxLength={400} onChange={e => setReason(e.target.value)}/></label><button disabled={busy || !reason.trim()} className="primary">Save reviewed revision</button>{error && <p role="alert">{error}</p>}</form>
}

export function ImageReviewForm({job, saved}: {job: Job; saved: (job: Job) => void}) {
  const original = (job.latest as ImageRevision | null)?.extraction; const base: ImageExtraction = original ?? {standing_water: 'not_assessable', visible_surface_damage: 'not_assessable', passage_obstruction: 'not_assessable', image_quality: 'limited', visible_description: '', uncertainties: imageFields.map(field => ({field, reason: 'limited_visibility'})), support_descriptions: []}; const [value, setValue] = useState<ImageExtraction>(structuredClone(base)); const [reviewer, setReviewer] = useState(reviewerLabel(job.original.operator)); const [reason, setReason] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false)
  function change(field: ImageField, state: ImageState) {const next = structuredClone(value); next[field] = state; next.uncertainties = next.uncertainties.filter(u => u.field !== field); next.support_descriptions = next.support_descriptions.filter(s => s.field !== field); if (state === 'present' || state === 'absent') next.support_descriptions.push({field, description: ''}); else next.uncertainties.push({field, reason: state === 'uncertain' ? 'ambiguous_visual' : 'limited_visibility'}); setValue(next)}
  function changeQuality(quality: ImageExtraction['image_quality']) {const next = structuredClone(value); next.image_quality = quality; if (quality === 'unusable') {for (const field of imageFields) next[field] = 'not_assessable'; next.support_descriptions = []; next.uncertainties = imageFields.map(field => ({field, reason: 'poor_quality'}))} setValue(next)}
  async function submit(e: React.FormEvent) {e.preventDefault(); setBusy(true); setError(''); try {saved(await api<Job>(`signals/${job.signal_id}/review`, {extraction: value, reviewer, reason, expected_revision: job.revision}))} catch (e) {setError(String(e))} finally {setBusy(false)}}
  return <form className="review-form image-review-form" onSubmit={submit}><h3>Human image review · new revision</h3><p>Review the visible pixels only. This records a label correction; it does not verify physical cause, depth, severity, or field condition.</p><div className="observation-grid">{imageFields.map(field => <label key={field}>{imageLabel(field)}<select aria-label={`Correct ${imageLabel(field)}`} value={value[field]} onChange={e => change(field, e.target.value as ImageState)}>{['present', 'absent', 'uncertain', 'not_assessable'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label>)}<label>Image quality<select value={value.image_quality} onChange={e => changeQuality(e.target.value as ImageExtraction['image_quality'])}>{['usable', 'limited', 'unusable'].map(s => <option key={s} value={s}>{human(s)}</option>)}</select></label></div><label>Visible description<input required maxLength={400} value={value.visible_description} onChange={e => setValue({...value, visible_description: e.target.value})}/></label>{value.support_descriptions.map((support, i) => <label key={support.field}>{imageLabel(support.field)} support description<input required maxLength={240} value={support.description} onChange={e => {const next = structuredClone(value); next.support_descriptions[i].description = e.target.value; setValue(next)}}/></label>)}<label>Reviewer<input required value={reviewer} maxLength={80} onChange={e => setReviewer(e.target.value)}/></label><label>Correction reason<input required value={reason} maxLength={400} onChange={e => setReason(e.target.value)}/></label><button disabled={busy || !reason.trim()} className="primary">Save reviewed image labels</button>{error && <p role="alert">{error}</p>}</form>
}

export function ImageResult({job}: {job: Job}) {const x = (job.latest as ImageRevision | null)?.extraction; if (!x) return null; return <div className="image-result"><table><tbody>{imageFields.map(field => <tr key={field}><th>{imageLabel(field)}</th><td><b>{human(x[field]).toUpperCase()}</b>{x.support_descriptions.filter(s => s.field === field).map(s => <small key={s.field}>{s.description}</small>)}</td></tr>)}<tr><th>Image quality</th><td><b>{x.image_quality.toUpperCase()}</b></td></tr></tbody></table><p className="visible-description"><b>Visible description</b><br/>{x.visible_description}</p>{x.uncertainties.map(u => <p className="muted" key={u.field}>{imageLabel(u.field)}: {human(u.reason)}</p>)}</div>}

export function ObservationPanel({onChanged}: {onChanged: () => void}) {
  const [jobs, setJobs] = useState<Job[]>([]); const [job, setJob] = useState<Job | null>(null); const [editing, setEditing] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [usage, setUsage] = useState<Record<string, unknown>>({}); const [text, setText] = useState(''); const [capture, setCapture] = useState<string>(crypto.randomUUID()); const [submissionKey, setSubmissionKey] = useState<string>(crypto.randomUUID()); const [independent, setIndependent] = useState(false); const [synthetic, setSynthetic] = useState(false); const [operator, setOperator] = useState(''); const [file, setFile] = useState<File | null>(null); const [preview, setPreview] = useState(''); const [imageOrigin, setImageOrigin] = useState<'collected' | 'public_source' | 'synthetic'>('collected'); const [sourceRef, setSourceRef] = useState(''); const [license, setLicense] = useState('local operator supplied')
  const localNow = () => {const d = new Date(); return new Date(d.getTime() - d.getTimezoneOffset() * 60000).toISOString().slice(0,16)}; const [observed, setObserved] = useState(localNow); const [lat, setLat] = useState('30.054'); const [lon, setLon] = useState('31.336'); const [road, setRoad] = useState(''); const [accuracy, setAccuracy] = useState('10')
  async function matchRoad() {setBusy(true); setError(''); try {const match = await api<{road_context_id: string | null; reason: string}>(`context/road-match?lon=${encodeURIComponent(lon)}&lat=${encodeURIComponent(lat)}&accuracy_m=${encodeURIComponent(accuracy)}`); setRoad(match.road_context_id ?? ''); if (!match.road_context_id) setError(`Road assignment needs review: ${match.reason.replaceAll('_', ' ')}. Enter a manually reviewed segment if justified.`)} catch (e) {setRoad(''); setError(String(e))} finally {setBusy(false)}}
  async function refresh() {setJobs(await api<Job[]>('observations')); setUsage(await api<Record<string, unknown>>('perception/usage'))}; useEffect(() => {refresh().catch(e => setError(String(e)))}, []); useEffect(() => {if (!file) {setPreview(''); return}; const url = URL.createObjectURL(file); setPreview(url); return () => URL.revokeObjectURL(url)}, [file])
  const running = job?.status === 'pending' || job?.status === 'analyzing'; useEffect(() => {if (!job || !running) return; let canceled = false; const timer = setTimeout(async () => {try {const next = await api<Job>(`signals/${job.signal_id}/processing`); if (!canceled) {setJob(next); if (!['pending', 'analyzing'].includes(next.status)) {await refresh(); onChanged()}}} catch (e) {if (!canceled) setError(String(e))}}, 800); return () => {canceled = true; clearTimeout(timer)}}, [job, running, onChanged])
  function chooseFile(e: React.ChangeEvent<HTMLInputElement>) {const chosen = e.target.files?.[0] ?? null; setError(''); if (!chosen) {setFile(null); return}; if (!['image/jpeg', 'image/png'].includes(chosen.type) || chosen.size > 5 * 1024 * 1024) {setFile(null); e.target.value = ''; setError('Choose a JPEG or PNG no larger than 5 MB.'); return}; setFile(chosen)}
  async function submit(e: React.FormEvent) {e.preventDefault(); setBusy(true); setError(''); setEditing(false); try {const metadata = {text: text.trim() || null, lat: Number(lat), lon: Number(lon), observed_at: new Date(observed).toISOString(), location_accuracy_m: Number(accuracy), road_context_id: road, capture_group_id: capture, independence: independent ? 'asserted' : 'uncertain', operator, content_origin: file ? imageOrigin : (synthetic ? 'synthetic' : 'collected'), source_ref: file && imageOrigin === 'public_source' ? sourceRef : undefined, license: file && imageOrigin === 'public_source' ? license : undefined, placement_origin: file && imageOrigin === 'synthetic' ? 'simulated' : undefined, time_origin: file && imageOrigin === 'synthetic' ? 'simulated' : undefined, idempotency_key: submissionKey}; let next: Job; if (file) {const result = await uploadImage<Job | {image_job: Job}>(file, metadata); next = 'image_job' in result ? result.image_job : result} else next = await api<Job>('signals', {...metadata, text: text.trim()}); setJob(next); setSubmissionKey(crypto.randomUUID()); setCapture(crypto.randomUUID()); setFile(null); setText(''); await refresh(); onChanged()} catch (e) {setError(String(e))} finally {setBusy(false)}}
  async function reanalyze(action: string) {if (!job) return; setBusy(true); setError(''); setEditing(false); try {setJob(await api<Job>(`signals/${job.signal_id}/reanalyze`, {action, expected_revision: job.revision}))} catch (e) {setError(String(e))} finally {setBusy(false)}}
  const selectedImage = job ? isImageJob(job) : false; const pairedId = job?.paired_text_signal_id ?? job?.linked_text_signal_id; const textJob = job?.paired_text_job ?? (pairedId ? jobs.find(item => item.signal_id === pairedId) : null); const x = !selectedImage ? (job?.latest as Revision | null)?.extraction : null; const sourceLabel = (original: Job['original']) => `${human(original.content_origin)} content · ${human(original.placement_origin ?? 'unknown')} placement · ${human(original.time_origin ?? 'unknown')} time`
  return <section className="observation-panel" aria-label="Text and image observation workspace">
    <div className="observation-columns">
      <form onSubmit={submit}><h2>New observation</h2>
        <label>Report text<textarea required={!file} maxLength={4000} rows={4} dir="auto" value={text} onChange={e => setText(e.target.value)} placeholder={file ? 'Optional report accompanying this image' : undefined}/></label>
        <label>Infrastructure image (optional)<input aria-label="Infrastructure image" type="file" accept="image/jpeg,image/png" onChange={chooseFile}/></label>
        {preview && file && <div className="image-preview"><img src={preview} alt="Selected infrastructure preview"/><button type="button" onClick={() => setFile(null)}>Remove image</button><small>JPEG/PNG · {(file.size / 1024 / 1024).toFixed(2)} MB · analysis starts only after submit</small></div>}
        {file && <label>Image source<select value={imageOrigin} onChange={e => setImageOrigin(e.target.value as typeof imageOrigin)}><option value="collected">Collected photo</option><option value="public_source">Public source</option><option value="synthetic">Synthetic demo photo</option></select><small>Public photos retain public-source provenance. Synthetic is reserved for demo placement/time.</small></label>}
        {file && imageOrigin === 'public_source' && <div className="observation-grid"><label>Public source reference<input required maxLength={500} value={sourceRef} onChange={e => setSourceRef(e.target.value)} placeholder="URL, consent record, or asset ID"/></label><label>License / permission<input required maxLength={240} value={license} onChange={e => setLicense(e.target.value)}/></label></div>}
        <div className="observation-grid"><label>Latitude<input required type="number" step="any" min="30.045" max="30.063" value={lat} onChange={e => setLat(e.target.value)}/></label><label>Longitude<input required type="number" step="any" min="31.325" max="31.346" value={lon} onChange={e => setLon(e.target.value)}/></label><label>Observation time (device local)<input required type="datetime-local" value={observed} onChange={e => setObserved(e.target.value)}/></label><label>Location accuracy (m)<input required type="number" min="0" max="500" value={accuracy} onChange={e => setAccuracy(e.target.value)}/></label><label>Reviewed road segment label<input required maxLength={80} value={road} onChange={e => setRoad(e.target.value)}/><button type="button" disabled={busy} onClick={matchRoad}>Match sourced road</button><small>Bounded OSM match; ambiguous pins require manual review.</small></label><label>Operator<input required maxLength={80} value={operator} onChange={e => setOperator(e.target.value)}/></label></div>
        <p className="muted">Confirm coordinates within the Nasr City study extent. Use the same segment label only for the same reviewed road. Road class and rainfall remain unknown.</p><label>Capture group<input required maxLength={80} value={capture} onChange={e => setCapture(e.target.value)}/></label><label className="inline-check"><input type="checkbox" checked={independent} onChange={e => setIndependent(e.target.checked)}/> I reviewed the source and assert this capture is independent; copies share a capture group.</label><label className="inline-check"><input type="checkbox" checked={synthetic} onChange={e => setSynthetic(e.target.checked)}/> This is a synthetic demo report with simulated placement and time.</label>
        <button className="primary" disabled={busy || running || (!text.trim() && !file) || !operator.trim() || !road.trim()}>{file ? 'Submit image and report' : 'Submit for text analysis'}</button><p className="muted">Analysis runs only on submission or an explicit review action. Image text is untrusted content and cannot issue instructions.</p>
      </form>
      <div><h2>Extraction and review</h2>
        <label>Saved observation<select aria-label="Saved observation" value={job?.signal_id ?? ''} onChange={async e => {if (!e.target.value) return; setJob(await api<Job>(`signals/${e.target.value}/processing`)); setEditing(false)}}><option value="">Select a saved report or image</option>{jobs.map(j => <option key={j.signal_id} value={j.signal_id}>{isImageJob(j) ? 'Image' : (j.original.text ?? '').slice(0,35)} · {human(j.status)}</option>)}</select></label>
        {job && <article className="extraction"><p role="status"><b>{human(job.status).replace(/^./, s => s.toUpperCase())}</b> · revision {job.revision}{selectedImage ? ' · image' : ' · text'}</p>
          {job.error_code && <p role="alert">{errorText[job.error_code] ?? 'Processing failed. Review manually or retry explicitly.'}</p>}
          {selectedImage && imageUrl(job) && <div className="saved-image"><a href={imageUrl(job)!} target="_blank" rel="noreferrer"><img src={imageUrl(job)!} alt="Saved infrastructure source"/></a><small>Open source image in a new tab</small></div>}
          <h3>{selectedImage ? 'Source image' : 'Original report'}</h3><small>{sourceLabel(job.original)}</small>{!selectedImage && <blockquote dir="auto">{job.original.text}</blockquote>}{selectedImage && job.original.text && <blockquote dir="auto">{job.original.text}</blockquote>}
          {job.latest && <><p><b>{job.latest.source === 'manual' ? 'Human reviewed' : job.latest.source === 'cached' ? 'Cached AI extraction' : 'Live AI extraction'}</b> · {job.latest.model ?? (selectedImage ? 'GPT-5.6 Luna' : 'operator')}<br/><small>Produced {new Date(job.latest.processed_at).toLocaleString()} · {human(job.latest.review_state)}{selectedImage && (job.latest as ImageRevision).cache_status ? ` · ${(job.latest as ImageRevision).cache_status}` : ''}</small></p>{job.latest.fallback_used && <p className="fallback-note">Terra fallback used: {human(job.latest.fallback_reason ?? '')}. Primary output is retained in revision details.</p>}
            {selectedImage ? <ImageResult job={job}/> : x && <><p>Language: {human(x.language)} · episode: {x.temporal_status}</p><table><tbody>{conditions.map(f => <tr key={f}><th>{human(f)}</th><td><b>{human(x[f]).toUpperCase()}</b>{x.evidence_spans.filter(s => s.field === f).map((s, i) => <p key={i} dir="auto">“{s.quote}” <small>({s.temporal_status})</small></p>)}</td></tr>)}<tr><th>Duration</th><td>{x.reported_duration ? `${x.reported_duration.minutes === null ? 'Unquantified' : '~' + x.reported_duration.minutes + ' minutes'} · “${x.reported_duration.phrase}”` : 'Not mentioned'}</td></tr><tr><th>Reported explanation</th><td>{x.reported_explanation ?? 'None'}<small>Reporter claim; never verified cause.</small></td></tr></tbody></table>{x.uncertainties.map((u,i) => <p key={i}>{human(u.field)}: {human(u.reason)}{u.affects_admission ? ' · admission uncertain' : ''}</p>)}</>}
            <details><summary>Revision history and provenance ({job.revisions.length})</summary><pre>{JSON.stringify(job.revisions, null, 2)}</pre></details></>}
          {textJob && <p className="paired-job"><b>Paired report</b> · {human(textJob.status)} · {textJob.latest?.source === 'cached' ? 'cached extraction' : textJob.latest ? 'extracted' : 'awaiting extraction'}<br/><small>Text and image from this submission share a capture group and are not counted as two witnesses.</small></p>}
          <p><b>Evidence status: {human(job.disposition)}</b></p>{job.disposition === 'not_admitted' && <p>{job.excluded.flatMap(e => e.rule_ids).map(human).join('; ') || 'No admitted extraction yet.'}</p>}
          {!running && <div className="review-actions"><button disabled={busy} onClick={() => setEditing(!editing)}>{selectedImage ? 'Correct image labels' : 'Correct extraction'}</button><button disabled={busy} onClick={() => reanalyze('retry_primary')}>{selectedImage ? 'Retry image analysis / use cache' : 'Retry primary / use cache'}</button><button disabled={busy} onClick={() => reanalyze('deeper_review')}>{selectedImage ? 'Deeper image review with Terra' : 'Deeper review with Terra'}</button></div>}
          {editing && (selectedImage ? <ImageReviewForm key={job.signal_id + ':' + job.revision} job={job} saved={next => {setJob(next); setEditing(false); refresh().catch(e => setError(String(e))); onChanged()}}/> : <ReviewForm key={job.signal_id + ':' + job.revision} job={job} saved={next => {setJob(next); setEditing(false); refresh().catch(e => setError(String(e))); onChanged()}}/>)}
        </article>}
      </div>
    </div>
    {error && <p className="error" role="alert">{error}</p>}<details><summary>Local API usage</summary><pre>{JSON.stringify(usage, null, 2)}</pre></details>
  </section>
}
